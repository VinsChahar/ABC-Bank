import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
    root: {
        margin: '2.4em',
        width: '100%',
        height: "100%",
        display: 'flex',
    },
}));